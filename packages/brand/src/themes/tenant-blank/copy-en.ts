/**
 * Tenant-blank — English copy scaffold.
 *
 * Copy this file when spinning up a new tenant theme. Replace every value
 * below with tenant-specific strings. Pages never reference these fields
 * directly — they consume the active theme's copy via the brand config.
 */
import type { ThemeCopy } from '../types';

export const tenantBlankCopyEn: ThemeCopy = {
  productName: 'Tenant Product',
  productShortName: 'Tenant',
  tagline: 'Tagline Goes Here',
  platformDescription: 'Short platform description for sign-in and about screens.',
  organizationName: 'Tenant Organization',
  legalFooter: '\u00A9 Tenant Organization',
};
